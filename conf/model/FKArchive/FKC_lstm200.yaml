factory_type: callbacks
model_type: LSTM
name: LSTM
learning_rate: 1e-4

input_size: 81
hidden_size: 531
output_size: 9

activations: False

callbacks:
    loss: 
        func: MSELoss
        idx_list: "asteval::range(81+531, 81+531+9)"
        sequence_position: last
    initialization:
        func: zeros
        size: "asteval::81+531+9"
    data: 
        func: insert
        idx_list: "asteval::range(81)"
        flatten_input: True
    output: 
        func: all
        idx_list: "asteval::range(81+531, 81+531+9)"
